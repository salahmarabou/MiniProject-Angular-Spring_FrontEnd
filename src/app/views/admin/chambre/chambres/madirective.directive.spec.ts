import { MadirectiveDirective } from './madirective.directive';

describe('MadirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new MadirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
